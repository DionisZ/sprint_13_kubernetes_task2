# Public example credentials for a disposable training cluster ONLY.
# Do not use these values for real accounts or sensitive data.
apiVersion: v1
kind: Secret
metadata:
  name: webapp-secret
type: Opaque
stringData:
  db-password: "lab-only-password"
  database-url: "postgres://webapp:lab-only-password@postgres:5432/webapp"
  rails-master-key: "47800c6168b491b3cd9c060f229fa472"
